name: zlib-build
on:
  push:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'ABC'
        required: false
        default: 'false'
      
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: madler/zlib
          ref: v1.2.12
          path: zlib
        
      - name: Check Environment
        run: |
          export
          pwd
          whoami
          lsb_release -a
          uname -a
          uptime
          ls
          
      - name: Configure
        run: |
          pwd
          cd zlib
          ./configure
          
      - name: Compile
        run: |
          pwd
          cd zlib
          make
          
