name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: madler/zlib
          ref: v1.2.12
        
      - name: my-step
        run: 
          uname -a
          lsb_release -a
          pwd
          ls

