name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: uname
        run: uname -a
      - name: pwd
        run: pwd
      - name: ls
        run: ls
